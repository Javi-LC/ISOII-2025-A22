package org.Component_3.server.domain;

public class ControlCatalog {

    public boolean existsByISBN(String isbn) {
        // Método vacío — consulta a persistencia iría aquí
        return false;
    }
}
